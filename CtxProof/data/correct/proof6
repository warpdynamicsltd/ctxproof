. ( ![X]: (a(X, Y) => b(Y)) ) => ( ( ?[X]: a(X, Y) ) => b(Y) )
{
    0 (?[X]: a(X, Y)) => b(Y)
    {
        0.0 ![X]: (a(X, Y) => b(Y)) {ASM} {.};
        0.1 ?[X]: a(X, Y) {ASM} {0};
        0.2 a(sk.0.2, Y) {R:SKO} {0.1} {sk.0.2, X};
        0.3 ( ![X]: (a(X, Y) => b(Y)) ) => ( a(sk.0.2, Y) => b(Y) ) {A:ALL} {a(X, Y) => b(Y)} {sk.0.2, X};
        0.4 a(sk.0.2, Y) => b(Y) {R:MOD} {0.3; 0.0};
        0.5 b(Y) {R:MOD} {0.4; 0.2};
    }
}